<Page x:Class="NeAccounting.Views.Pages.Test.TestPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:local="clr-namespace:NeAccounting.Views.Pages.Test"
      xmlns:nu="clr-namespace:NeAccounting.Controls"
      d:DataContext="{d:DesignInstance local:TestPage,
                                     IsDesignTimeCreatable=False}"
      mc:Ignorable="d"
      Height="800"
      Width="600"
      Title="TestPage">

    <Grid>
        <StackPanel Width="400">
            

            <nu:PersianDatePicker SelectedDate="{Binding ViewModel.Tse, Mode=TwoWay}"
                                  DisplayDate="... تاریخ امروز"
                                  LabelName="تاریخ را وارد کنید"/>
        <Button Command="{Binding ViewModel.MyClickCommand}">clickMe</Button>
        </StackPanel>
    </Grid>
</Page>
